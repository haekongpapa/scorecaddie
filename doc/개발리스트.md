# ScoreCaddie 개발 리스트 (화면별 기능 상세 + 구현 현황, 2026-07-20 갱신)

근거 문서: `doc/pages.md`, `doc/mockups/`, `doc/ScoreCaddie_분석설계_요약.pptx`, `app/prisma/schema.prisma`, `doc/admin-csv-upload.md`
구현 여부는 이번에 `app/src` 실제 코드를 직접 확인한 결과 기준. 범례: ✅ 완료 / 🟡 부분 구현 / ⬜ 미구현

## ⚠️ 확인 중 발견한 사항 (memory.md 이력과 실제 코드 불일치)

- `app/src/app/login/page.tsx`, `signup/page.tsx`, `page.tsx`(랜딩)는 전부 "OO 화면 (구현 예정)" 안내 문구만 있는 placeholder였음 — 예전 세션 로그(memory.md 8번)에는 "로그인/회원가입 페이지 구현 완료"로 남아있으나 실제로는 폼/버튼/`signIn()` 연동이 전혀 없는 상태.
- 골프장 공공데이터 연동 코드(`src/lib/public-data/`, `scripts/sync-golf-courses.ts`, 좌표 변환 유틸)가 프로젝트 폴더에 전혀 존재하지 않음 — `app/README.md`에 "샌드박스 임시환경에서 검증(PC에는 남지 않음)"이라 적혀 있어, 예전에 검증만 하고 실제 파일은 저장된 적이 없었던 것으로 보임. **처음부터 다시 작성 필요.**
- 아래 리스트의 "미구현" 항목들은 이 재확인을 반영한 것이며, 이전에 작성한 개발리스트(요약본)보다 범위가 넓음.

---

## 0. 기반 작업 현황 (화면과 별개)

| 항목 | 상태 | 비고 |
|---|---|---|
| Prisma DB 스키마 (6개 핵심 테이블 + NextAuth 4개) | ✅ | 로컬 마이그레이션 6건 전부 적용 확인 |
| NextAuth 설정 (Credentials/Google/Kakao provider 등록) | ✅ | 로그인 화면에서 `signIn()` 실제 연동 완료(2026-07-20) |
| 보호 라우트 미들웨어 | ✅ | `/admin/*` 추가 완료(2026-07-20). **Edge 런타임 crypto 오류 수정(2026-07-20)**: `auth.config.ts`(Edge-safe, Credentials/Prisma/bcrypt 제외)를 분리하고 미들웨어는 이걸로 별도 인스턴스 생성하도록 변경. **2026-07-22 정정**: role=ADMIN 체크(AdminGuard)도 이미 완료된 상태였음 — 미들웨어가 JWT의 role로 `/admin/*` 접근을 1차 차단하고, 각 관리자 페이지가 세션에서 다시 한 번 확인하는 이중 검사 구조로 11~14번 구현 시 함께 반영되어 있었음(이 표의 문구가 갱신 안 되고 "예정"으로 잘못 남아있었음) |
| 회원가입 API (`/api/signup`) | ✅ | `thirdPartyConsent` 저장 로직 추가 + 회원가입 화면 연동 완료(2026-07-20) |
| 골프장 공공데이터 연동(동기화 스크립트, 좌표 변환) | ✅ | **2026-07-22 정정**: 11번 관리자 화면의 `POST /api/admin/golf-courses/sync`로 재구현 완료(2026-07-20) — data.go.kr 실시간 API 직접 호출 + upsert 시점에 TM→WGS84(proj4) 변환까지 처리. 이 표는 6번 항목(당시 코드가 실제로 없었던 시점) 작성 이후 갱신이 안 돼 있었음 |
| 기상청 단기예보 API 연동 | ✅ | 2026-07-22 구현 완료 — `src/lib/weather/`(격자좌표 변환 + getVilageFcst 클라이언트), 7-1 미리보기 API, `POST /api/rounds` 스냅샷 저장. `WEATHER_API_KEY`는 아직 빈 값이라 실제 발급 후 로컬에서 최종 확인 필요 |
| 좌표 결측 지오코딩 배치 | ✅ | 2026-07-22 구현 완료 — 11번 화면 "좌표 지오코딩 실행" 카드, `src/lib/geocoding/kakao.ts`(카카오 로컬 API, address→addressLotno→키워드 3단계 폴백, memory.md 82번). 잔여 실패 9건에 대해 공백 표기/신지명 미반영 가설을 세웠으나 사용자가 map.kakao.com에서 직접 검색해 둘 다 반증 — 실제 원인은 **공공데이터와 카카오맵에 등록된 지번 자체가 다름**(데이터 소스 간 불일치, 코드로 해결 불가)으로 결론(memory.md 85번). 관련 정규화 코드는 제거, 잔여 9건은 그대로 유지하기로 확정 |
| `tsconfig.json` 경로 별칭(`@/*`) 설정 | ✅ | `baseUrl`/`paths` 누락으로 `@/auth`,`@/lib/prisma` 등이 실제 빌드에서도 깨질 수 있던 버그 발견·수정(2026-07-20). `npx tsc --noEmit` 0 에러 확인 |
| 구글 OAuth 실제 로그인 | ✅ (2026-07-20) — `@auth/prisma-adapter` 연결 + 사용자가 Google Cloud Console에서 발급한 Client ID/Secret을 `.env`에 반영, 로컬에서 실제 로그인 성공 확인 완료 |
| 카카오 OAuth 로그인 | 삭제됨(2026-07-22) — 사용자 요청으로 기능 자체를 제거. 구글 로그인 + 이메일/비밀번호만 유지(memory.md 86번) |

---

## 1. 랜딩 (`/`)

| 기능 | 상태 |
|---|---|
| 히어로 섹션(서비스명 + 한 줄 소개) | ✅ |
| 핵심 기능 3~4개 카드 요약(골프장 검색/스코어 기록/날씨 연동/통계 확인) | ✅ |
| 로그인/회원가입 CTA 버튼 | ✅ |
| 로그인 상태면 `/dashboard` 자동 리다이렉트 (`auth()` 서버 체크) | ✅ |

## 2. 로그인 (`/login`)

| 기능 | 상태 |
|---|---|
| 이메일/비밀번호 입력 폼 | ✅ |
| Credentials `signIn()` 연동 | ✅ |
| 구글 소셜 로그인 버튼 + 연동 | ✅ (버튼/`signIn("google")` 연동 완료, 실제 로그인은 로컬에서 구글 OAuth 자격증명으로 확인 필요) |
| 카카오 소셜 로그인 버튼 + 연동 | ✅ (카카오도 동일) |
| 에러 메시지(잘못된 이메일/비밀번호) | ✅ |
| 제출 중 로딩(버튼 비활성화) | ✅ |
| 회원가입 페이지 링크 | ✅ |

## 3. 회원가입 (`/signup`)

| 기능 | 상태 |
|---|---|
| 이름/이메일/비밀번호/비밀번호 확인 입력 폼 | ✅ |
| 타인에게 정보 제공 동의 체크박스(선택, 기본 미체크) + 안내문구 | ✅ |
| `POST /api/signup` 화면 연동 | ✅ |
| `thirdPartyConsent` 값 API에 저장 | ✅ |
| 이메일 중복 에러 표시 | ✅ |
| 비밀번호 규칙 미달 에러(8자 미만/불일치) | ✅ |
| 제출 중 로딩 | ✅ |
| 로그인 페이지 링크 | ✅ |

## 4. 대시보드 (`/dashboard`)

| 기능 | 상태 |
|---|---|
| 인사말(사용자명) | ✅ |
| 메뉴 카드 4개(골프장/스코어등록/스코어조회/마이페이지) + 라우팅 | ✅ |
| `role=ADMIN` 관리자 배너 2개(골프장 Par 관리 / 회원 관리) | ✅ |
| 최근 라운드 3건 미리보기(골프장명·날짜·출발시간·총타수) | ✅ (2026-07-22 날짜 옆에 출발시간 추가 — `round.startTime` 있으면 "날짜 · 오전 09:00" 형식, 없으면 날짜만) |
| 목업 디자인 반영(아이콘 메뉴카드, 리스트형 관리자배너/최근라운드, 하단 고정 네비게이션) | ✅ (2026-07-20) — `NavBar`/`RoundListItem` 공통 컴포넌트 신설, `courses`/`rounds`/`profile` placeholder에도 동일하게 반영 |
| 상단 타이틀 바(뒤로가기/제목/우측 아이콘) | ✅ (2026-07-20) — `TopBar` 공통 컴포넌트 신설, 대시보드/골프장/스코어조회/마이페이지 4개 화면에 적용 |
| 빈 상태("첫 라운드를 등록해보세요") | ✅ |

## 5. 골프장 목록 (`/courses`)

> 2026-07-20 실제 구현 완료. 목업: `doc/mockups/05-courses.html`. 공공/민간 판정·주소 요약 로직은 실데이터 미검증 가정(`doc/pages.md` 5번 참고).

| 기능 | 상태 |
|---|---|
| 검색창(이름/지역) | ✅ (`CourseSearchList`, 클라이언트 필터) |
| 필터(전체/공공·민간) | ✅ — `publicPrivate` 문자열 기반 추정 분류(실데이터 검증 필요) |
| 골프장 카드 리스트 | ✅ |
| 페이지네이션 | ✅ — "더 보기" 버튼(20개씩 load-more), 번호 페이지네이션 아님 |
| 좌표 결측 "위치 확인 중" 배지 | ✅ — `latitude`/`longitude` null 여부로 판단. 2026-07-20부터 sync가 TM→WGS84 좌표 변환도 수행해서 대부분 채워짐(원본 좌표값 자체가 없는 소수만 남음) |
| 검색 결과 없음 상태 | ✅ |
| `GolfCourse` 목록 조회 쿼리 | ✅ (`prisma.golfCourse.findMany` + loops include) |
| 항목 클릭 → 6번 이동 | 🟡 (`/courses/[id]`로 링크 연결됨, 6번 화면 자체는 placeholder) |

## 6. 골프장 상세 (`/courses/[id]`) — 2026-07-21 실제 구현 완료

| 기능 | 상태 |
|---|---|
| 골프장명/주소/지도 표시 | ✅ |
| 좌표 없음 시 주소 텍스트만 표시 | ✅ (좌표 있으면 위경도 숫자, 없으면 "위치 확인 중" — 05번과 문구 통일) |
| "이 골프장에서 스코어 등록" 버튼 → 7-1 이동 | ✅ (`/rounds/new?courseId=<id>`, 7-1 자체는 아직 placeholder) |
| 이 골프장에서의 내 라운드 이력 리스트 | ✅ (`RoundListItem` 재사용, 날짜+출발시간+타수 — 2026-07-22 출발시간 추가) |
| `GolfCourse` 단건 조회 | ✅ (별도 REST API 없이 서버 컴포넌트에서 Prisma 직접 조회 — 05번과 동일 패턴) |
| `Round`(golfCourseId+userId) 조회 | ✅ |
| id 매칭 실패 시 폴백 처리 | ✅ (설계 변경: 목업은 `?course=<이름>` 문자열 매칭이었으나, 05번이 이미 실제 `golfCourseId`로 링크하므로 경로 파라미터(`/courses/[id]`)를 그대로 사용. 존재하지 않는 id는 `notFound()`로 404 처리) |

## 7-1. 스코어 등록 Step1 (`/rounds/new`)

> `Round.startTime` 마이그레이션(`20260721090000_add_round_start_time`) 적용 + `npx prisma generate` 로컬 실행 완료(2026-07-21, 사용자 확인) — `npx tsc --noEmit` 클린 확인됨.

| 기능 | 상태 |
|---|---|
| 골프장 콤보박스 | ✅ |
| 9H/18H 세그먼트 토글 | ✅ |
| 전반/후반 코스(루프) 선택, 9H면 후반 숨김 | ✅ |
| 루프 미등록 골프장 "코스 정보 없음" 안내 | ✅ |
| 라운드 일자 피커 | ✅ |
| 출발 시간(Starting Time, 오전/오후+시 1~12+분) 입력 | ✅ (목업과 동일한 커스텀 피커를 실구현에도 반영, `Round.startTime`에 "HH:MM" 24시간제로 저장) |
| 동일 골프장·일자·출발시간 중복 라운드 등록 방지 | ✅ ("스코어 카드" 버튼 클릭 시 `GET /api/rounds/check-duplicate`로 사전 확인해 중복이면 Step2 진입 자체를 막고 안내 메시지 표시. `POST /api/rounds`에도 동일 검증을 최종 방어선으로 한 번 더 수행) |
| 날씨 자동 표시 카드 | ✅ (2026-07-22 실구현 — `GET /api/weather/preview`로 골프장/일자/출발시간 변경 시 400ms 디바운스 후 재조회. 예보 제공 범위(오늘~+3일) 밖이거나 API 키 미설정/호출 실패 시 "날씨 정보 없음"으로 표시) |
| `?courseId=` 진입 시 골프장 미리 선택 | ✅ (목업의 `?course=`(이름) 대신 실제 DB id인 `courseId` 사용 — 06번 골프장 상세 진입 버튼과 일치) |
| "스코어 카드" 버튼 → 7-2 이동(파라미터 전달) | ✅ (`step=2` 파라미터로 Step1/Step2 분기, `courseId/holesPlayed/date/startTime/frontLoopId/backLoopId` 전달) |
| `GolfCourseLoop` 목록 조회 API | ✅ (별도 API 대신 `/rounds/new` 서버 컴포넌트에서 Prisma 직접 조회 — 05/11번과 동일 패턴) |

## 7-2. 스코어 등록 Step2

**설계 변경(2026-07-21): 목업의 `scores=` URL파라미터 임시전달 방식 대신, "첫 홀 저장" 시점에 실제 `Round`를 DB에 생성하고 이후 각 홀 저장마다 `HoleScore`를 upsert하는 방식으로 구현.** 이렇게 하면 "라운드 상세" 버튼이 항상 실제 `roundId`(`/rounds/[id]`)로 연결되고, 수정 모드도 `edit=<roundId>` 기반 서버 조회로 목업의 한계(구버전 라운드는 티샷결과 등 상세 필드 유실) 없이 전체 필드를 정확히 복원한다. 라운드 생성 직후 URL을 `router.replace`로 `?step=2&edit=<roundId>`로 교체해 새로고침 시 중복 라운드 생성을 방지.

| 기능 | 상태 |
|---|---|
| 전반/후반 스코어카드 표 + 홀 탭 이동 | ✅ |
| PAR 3/4/5/6 버튼(즉시 변경) | ✅ |
| 티샷결과 선택(페어웨이/실패/페널티/OB) | ✅ |
| 페널티/OB 선택 시 타수 자동 가산(+1/+2) 및 되돌리기 | ✅ |
| 페널티·OB 타수 스테퍼 + 그린주변벙커 체크박스 | ✅ |
| 온그린 핀거리(0~20m 셀렉트 / 20m 이상 버튼) | ✅ |
| 온그린·퍼트 타수 스테퍼 + 오버파 자동계산 표시 | ✅ |
| 홀메모 입력(100자 제한) | ✅ |
| 홀 저장 → 다음 홀 자동 이동 | ✅ (저장 시 `Round`(최초 1회)+`HoleScore` upsert API 호출, 성공 시에만 다음 홀로 이동) |
| 이전/다음/초기화 네비게이션 | ✅ |
| "라운드 상세" 링크(저장 합계 반영) → 9번 이동 | ✅ (실제 `roundId`로 `/rounds/[id]` 이동. 첫 홀 저장 전에는 비활성) |
| 수정 모드(`roundId` 기반 서버 조회) | ✅ (`?step=2&edit=<roundId>` — 목업의 `scores=` 대신 정식 구현. 9번 라운드 상세 화면에 "수정" 진입 버튼은 아직 없음 — 9번 구현 시 연결 예정) |
| `Round` 생성 API | ✅ (`POST /api/rounds`, 생성 전 중복 라운드 검증 포함) |
| `HoleScore` 생성/수정 API | ✅ (`PUT /api/rounds/[id]/holes/[holeNumber]`, upsert) |
| 출발 시간 표시(정보 바) | ✅ (`[ 일자(요일) 골프장 18H · 오전 09:00 출발 ]` 형식) |
| 날씨 스냅샷 저장(`weatherSnapshot`) | ✅ (2026-07-22 — `POST /api/rounds`가 Round 생성 시 골프장 좌표+`playedAt`(+`startTime`)으로 한 번 캡처해 고정 저장. 예보 범위 밖인 과거 라운드는 여전히 null이 정상) |

## 8. 스코어 조회 (`/rounds`) — 2026-07-22 실제 구현 완료

> **실제 구현 설계 변경**: 목업/`doc/pages.md`는 리스트 항목 클릭 시 `course`/`date`/`score`/`weather`/`ownerId`/`owner`를 쿼리로 9번에 전달했지만, 9번이 이미 `roundId` 하나만으로 직접 조회하는 방식으로 바뀌어 있어(72번 항목) 여기서도 `/rounds/[id]`로 단순 링크. 별도 조회 API(`GET /api/rounds?...`) 없이 서버 컴포넌트에서 Prisma를 직접 호출(다른 화면과 동일 패턴), 필터는 클라이언트 컴포넌트(`RoundSearchList`)에서 서버가 내려준 전체 목록을 대상으로 처리(05번 골프장 목록의 `CourseSearchList`와 동일한 패턴). 골프장 select는 전체 골프장(~650건) 대신 "조회 가능한 라운드가 실제로 있는 골프장"만 옵션으로 좁힘.

| 기능 | 상태 |
|---|---|
| 기간 range 날짜 피커 2개 | ✅ |
| 골프장 select(전체/특정) | ✅ (전체 골프장이 아닌 "라운드 기록이 있는 골프장"만 노출 — 설계 변경) |
| 사용자 범위 세그먼트(내 기록만/전체 회원, 전환 즉시 재조회) | ✅ |
| 검색/초기화 버튼 | ✅ (기간/골프장은 검색 버튼으로 적용, 사용자범위는 즉시 적용 — 스펙 그대로) |
| 전체 건수 표시 | ✅ |
| 라운드 리스트(골프장명/날짜·출발시간/날씨/총타수) | ✅ (73번에서 추가된 출발시간도 날짜와 함께 표시, `formatRoundDateTimeLabel` 재사용) |
| 전체 회원 모드 시 사용자 이름 추가 표시 | ✅ |
| 빈 상태 2종(내 기록 없음 / 조건에 맞는 라운드 없음) | ✅ |
| `thirdPartyConsent` 게이팅 | ✅ (별도 API 없이 서버 컴포넌트 쿼리에서 `OR: [{userId}, {user.thirdPartyConsent:true}]`로 한 번에 처리) |
| 리스트 항목 클릭 → 9번 이동 | ✅ (쿼리 파라미터 없이 `/rounds/[id]`로 단순 링크 — 위 설계 변경 참고) |

## 9. 라운드 상세 (`/rounds/[id]`)

> **실제 구현(2026-07-21) 설계 변경**: 목업은 8번(스코어 조회)에서 `course`/`date`/`score`/`weather`/`ownerId`/`owner`를 URL 쿼리로 전달받아 화면을 구성했지만, 실제 구현은 URL의 실제 `roundId` 하나만으로 서버에서 `Round`+`HoleScore`+`GolfCourse`+`User`를 직접 조회한다(06번 골프장 상세의 "내 라운드 이력" 링크가 이미 실제 `roundId`로 연결돼 있어 그대로 사용 가능, 7-2번 "라운드 상세" 버튼도 동일).

| 기능 | 상태 |
|---|---|
| 상단 요약 카드(골프장/날짜/날씨/총타수) | ✅ (2026-07-22: 기상청 API 연동 완료로 예보 범위 내 라운드는 실제 날씨 라벨 표시, 범위 밖/키 미설정이면 여전히 "날씨 정보 없음". `startTime`이 있으면 "오전/오후 HH:MM 출발"도 함께 표시, 2026-07-21 추가) |
| 타인 라운드 "OOO님의 라운드 · 읽기 전용" 배지 | ✅ |
| 상단 고정 범례 바 | ✅ |
| 전반/후반 매트릭스 표(카드 분리 + 컬러바 구분) | ✅ |
| 홀/파/스코어 행 배경·굵기 구분 | ✅ |
| 티샷결과 칩(FW/MS/PN/OB) + GIR 점 표시 | ✅ |
| 스크램블링(S)/샌드세이브(✓·✗) 파생 계산 및 표시 | ✅ |
| 언더파/오버파 숫자 표시(버디 동그라미/보기 네모 등) | ✅ |
| 메모 아이콘 클릭 → 하단 패널에 메모 표시 | ✅ |
| 수정/삭제 버튼(본인 라운드만) | ✅ (수정 → `/rounds/new?step=2&edit=<roundId>`로 7-2 재사용, 삭제 → `DELETE /api/rounds/[id]` + 확인창) |
| 읽기 전용 안내문(타인 라운드, `thirdPartyConsent` 조건) | ✅ (동의하지 않은 회원의 라운드는 소유자 본인 외 접근 시 404 처리 — 목업엔 없던 방어 로직 추가) |
| `Round` 삭제 API | ✅ (`DELETE /api/rounds/[id]`, `HoleScore`는 `onDelete: Cascade`로 함께 삭제) |
| 소유자 검증 로직 | ⬜ |
| `Round`+`HoleScore`(18건) 조회 API | ⬜ |
| "수정" 버튼 → 7-2 이동(`roundId` 기반, `edit=1`) | ⬜ |
| 라운드 삭제 API | ⬜ |

## 10. 마이페이지 (`/profile`)

> 2026-07-20 화면/API 구현 완료 (아래 표 참고).

| 기능 | 상태 |
|---|---|
| 프로필 카드(이름/이메일/가입일) | ✅ (2026-07-20 가입일 추가 — mockup/pages.md/구현 전부 동기화 완료) |
| 통계 카드(총 라운드 수/평균 타수/베스트 스코어) | ✅ (mockup·pages.md 모두 "베스트 스코어" 기준으로 통일. 실제 라운드 데이터가 없어 현재는 0/-/-로 표시) |
| 타인 정보 제공 동의 토글 스위치(상태 텍스트 함께 표시) | ✅ (`ConsentToggle` 컴포넌트, 즉시 저장) |
| 비밀번호 변경 메뉴(소셜 전용 계정은 숨김, `passwordHash` null 체크) | ✅ (2026-07-20) — `/profile/change-password`(10-1) 화면 + `PATCH /api/me/password` API로 실제 기능 구현 완료 |
| 로그아웃 | ✅ (`LogoutListItem`, `signOut()`) |
| `PATCH /api/me` API(동의 토글 저장) | ✅ |
| `PATCH /api/me/password` API(비밀번호 변경) | ✅ (2026-07-20 신규) |
| 통계 집계 쿼리 | ✅ |

## 11. 관리자 - 골프장 Par 관리 목록 (`/admin/golf-courses`)

> 2026-07-20 실제 구현 완료(공공 데이터 업로드 기능 포함, 이후 실시간 API 방식으로 재구현). 목업: `doc/mockups/11-admin-courses.html`. 설계 문서: `doc/admin-golfcourse-sync.md`.

| 기능 | 상태 |
|---|---|
| role 서버사이드 차단(미들웨어 + 페이지 이중 검사) | ✅ (2026-07-20 — 기존 미들웨어가 로그인 여부만 검사하고 role은 검사 안 하던 버그도 함께 수정) |
| 골프장 공공 데이터 업로드 카드(최종 업데이트 표시 + 업로드 버튼) | ✅ (`PublicDataSyncCard`) |
| 공공 데이터 업로드 API(`POST /api/admin/golf-courses/sync`) | ✅ — data.go.kr 실시간 API를 서버가 직접 호출(`numOfRows=100` 페이지네이션, 페이지마다 즉시 upsert). 최초엔 파일데이터로 오판해 CSV 업로드 방식으로 만들었다가, 실제 API 스펙 확인 후 실시간 호출 방식으로 재구현(2026-07-20). **사용자가 로컬에서 실제 버튼 클릭으로 동작 확인 완료 — 신규 652개 추가(2026-07-20)**. 이후 TM→WGS84 좌표 변환(`proj4`)도 upsert 시점에 함께 처리하도록 추가(2026-07-20, `npm install` 후 재검증 필요) |
| 업로드 결과 토스트(신규/갱신 건수) | ✅ |
| "CSV 일괄 업로드" 버튼 → 13번 이동 | ✅ (버튼만 연결, 13번 화면 자체는 placeholder) |
| "CSV 내보내기" 버튼(현재 DB의 Par 데이터를 13번과 동일 포맷으로 다운로드) | ✅ (2026-07-21 신규 — `GET /api/admin/golf-courses/export`. 화면 목록에는 없던 항목이나 12번 작업 완료 후 사용자가 "DB에 저장된 코스별 Par 정보를 CSV로 저장해달라"고 요청해 추가. 미등록 홀(Par 없음)은 UI처럼 기본값을 채우지 않고 실제 저장된 값만 내보냄) |
| 검색창(이름/주소 클라이언트 필터) | ✅ (`GolfCourseAdminList`) |
| 골프장 목록 테이블(주소/루프 수/Par 등록 상태/최종 수정일) | ✅ |
| 루프 수 배지 | ✅ |
| Par 등록 상태 배지(완료 9/9·부분 n/9·미등록) | ❌ (2026-07-21 제거 — 사용자가 "화면이 깔끔하지 않다"며 목록 우측 배지 삭제 요청. 좌측 "루프 N개"/"루프 미등록" 배지만 남기고 우측 상태 배지는 삭제) |
| 행 클릭 → 12번 이동 | ✅ (2026-07-21 갱신 — 12번 화면 실제 구현 완료로 placeholder 아님) |
| 목록 조회 쿼리 | ✅ (`prisma.golfCourse.findMany` + loops/holes include) |
| "전체 / 코스 등록됨" 필터 탭 | ✅ (2026-07-21 — 목업 승인 후 실제 구현. `GolfCourseAdminList.tsx`에 05번 `CourseSearchList`와 동일한 칩 스타일로 추가, `loopNames.length > 0` 기준(Par 값 채움 여부와 무관). 라벨은 목업의 "코스 등록된 골프장만"에서 칩 스타일에 맞게 "코스 등록됨"으로 축약) |
| "좌표 지오코딩 실행" 카드(주소 기반 좌표 채우기) | ✅ (2026-07-22 신규 — `GeocodeBatchCard` + `POST /api/admin/golf-courses/geocode`. 카카오 로컬 API(주소 검색)로 `needsGeocoding=true`인 골프장의 `address`를 좌표로 변환. 서버리스 실행시간 대비 1회 최대 150건만 처리하고 나머지는 버튼 재클릭으로 이어서 처리(진행 상태는 별도 저장 없이 `needsGeocoding` 플래그 자체로 관리). 주소 자체가 없는 골프장은 애초에 대상에서 제외하고 건수만 안내. `KAKAO_REST_API_KEY`(신규 env, NextAuth 카카오 로그인 키와 별개) 미설정 시 실행 자체를 막고 안내 메시지 표시 |

## 12. 관리자 - 골프장 루프·Par 입력/수정 (`/admin/golf-courses/[id]/par`) — 2026-07-21 실제 구현 완료

| 기능 | 상태 |
|---|---|
| 루프 탭 목록(전반/후반/+ 추가) | ✅ |
| 루프 이름 인라인 편집 | ✅ (더블클릭 → prompt, 목업과 동일한 상호작용) |
| 루프 삭제(참조 중인 `Round` 있으면 경고) | ✅ (confirm 문구에 참조 라운드 건수 표시. 삭제는 막지 않음 — FK가 optional이라 Prisma 기본 동작상 참조만 SetNull로 해제되고 과거 `HoleScore.par` 스냅샷은 영향 없음) |
| 루프 드래그 재정렬(`sortOrder`) | ✅ (설계 변경: 드래그 대신 ▲▼ 버튼으로 인접 루프와 `sortOrder` 교체 — 새 의존성 라이브러리 추가 없이 동일 기능 제공) |
| "+ 루프 추가" | ✅ |
| 9홀 Par 입력 그리드(3/4/5) | ✅ (3열 그리드로 표시 — 목업의 2열 grid는 UI 배치만 다름) |
| 저장 버튼 + 미저장 변경 안내 | ✅ (Par 값 변경은 로컬 상태에만 반영되다가 "저장" 클릭 시 활성 루프 전체를 일괄 저장. 다른 루프 탭으로 이동 시 미저장 변경 있으면 confirm 경고) |
| `GolfCourseLoop` CRUD API | ✅ (`POST .../loops`, `PATCH`/`DELETE .../loops/[loopId]`) |
| `GolfCourseHole` upsert API | ✅ (`PUT .../loops/[loopId]/holes`, 9건 트랜잭션 upsert) |

## 13. 관리자 - CSV 일괄 업로드 (`/admin/golf-courses/upload`) — 2026-07-21 실제 구현 완료

| 기능 | 상태 |
|---|---|
| 포맷 안내(`골프장명,루프명,홀번호,Par`) | ✅ |
| 샘플 CSV 다운로드 링크 | ✅ (`public/sample-golfcourse-par.csv` 정적 파일 링크 — 별도 API 없이 Next.js가 `/sample-golfcourse-par.csv`로 바로 서빙) |
| 파일 업로드 드롭존 | ✅ (드래그앤드롭 + 탭하여 파일선택 둘 다 지원) |
| CSV 파싱 → 골프장명 매칭 → 루프명 매칭/자동생성 → `GolfCourseHole` upsert | ✅ (기존에 있었으나 미사용 상태였던 `lib/csv.ts`의 `parseCsv()` 재사용 — 11번 공공데이터 업로드를 CSV 방식으로 처음 만들었다가 실시간 API로 전환하며 남아있던 파일) |
| 처리 결과 리포트(행 번호·상태·오류 메시지) | 🟡 (설계 변경: 목업은 성공/실패/신규루프 행을 전부 나열하는 풀 테이블이었으나, `doc/admin-csv-upload.md`에 이미 확정된 응답 스펙(`totalRows`/`successCount`/`failCount`/`errors`)을 그대로 따라 **실패 행만** 표로 보여주고 성공은 건수로만 표시. "신규 루프 생성 건 강조 표시"는 그 문서의 "향후 보완 후보"에 남아있어 이번 범위에서 제외) |
| 업로드 처리 API | ✅ (`POST /api/admin/golf-courses/upload`, `doc/admin-csv-upload.md` 스펙 그대로 — 행 단위 개별 처리라 일부 행 오류가 나머지 정상 행에 영향 없음, 오류 목록은 응답 과다 방지로 상위 200건까지만 반환) |

## 14. 관리자 - 회원 관리 (`/admin/users`) — 2026-07-21 실제 구현 완료 (단일선택 UX로 갱신)

| 기능 | 상태 |
|---|---|
| AdminGuard | ✅ (session 체크 + `role !== ADMIN`이면 `/dashboard` 리다이렉트, 미들웨어 `/admin/:path*` 보호와 이중) |
| 이름 검색 + 가입일자 범위 필터 | ✅ (목업 그대로 "검색" 버튼 클릭 시 적용되는 방식 — 05/11번의 실시간 필터와 다름) |
| "관리자만 보기" 필터 | ✅ (2026-07-21 신규 — 체크박스, "검색" 클릭 시 다른 필터와 함께 적용) |
| 회원 그리드(라디오 단일선택/이름/이메일/가입일/어드민 컬럼) | ✅ (2026-07-21 — 다중선택 체크박스 → 라디오 버튼 단일선택으로 변경. 헤더 전체선택 체크박스는 단일선택 개념상 의미 없어져 제거) |
| 선택 회원 role에 따라 버튼 1개만 노출 | ✅ (2026-07-21 신규 — USER면 "어드민 권한 부여"만, ADMIN이면 "어드민 권한 해제"만. 기존엔 두 버튼이 항상 같이 떠서 실수로 반대 버튼을 누를 여지가 있었음) |
| `name`이 null인 계정 "이름 없음" 표시 | ✅ |
| 본인 계정 권한 자가 해제 처리 | ✅ (서버 차단 정책 유지 + 2026-07-21 UX 보강: 본인이 ADMIN 상태로 선택되면 "어드민 권한 해제" 버튼 자체를 비활성 텍스트("본인 계정은 해제 불가")로 바꿔 애초에 클릭 못 하게 함 — 이전엔 클릭은 되고 서버가 조용히 스킵하는 방식이었는데, 단일선택으로 바뀌며 클라이언트에서 먼저 막는 게 더 명확해짐) |
| 목록 조회 API | ✅ (별도 REST API 없이 서버 컴포넌트에서 Prisma 직접 조회 — 05/11번과 동일 패턴) |
| `PATCH /api/admin/users/role` 처리 API | ✅ (다중→단일선택으로 바뀌어도 API 자체는 변경 없음 — `userIds` 배열에 1개만 담아 호출) |

---

## 공통 컴포넌트

| 컴포넌트 | 상태 |
|---|---|
| NavBar(로그인 상태별/관리자 메뉴) | ⬜ |
| GolfCourseCard | ⬜ |
| RoundListItem | ⬜ |
| WeatherBadge | ⬜ |
| EmptyState | ⬜ |
| AdminGuard | ⬜ |

---

## 진행 전 확인 필요한 미정 사항 (memory.md 5번)

- 배포/호스팅 예산 및 도메인 여부
- 모바일 대응 범위(반응형 웹으로 충분한지)
- CSV 업로드 골프장명 매칭 정책(동일 이름 여러 개인 경우)
- 관리자 본인 권한 자가 해제 허용 여부
- 최초 관리자 계정 지정 방법

## 착수 순서 제안

1. ~~(버그성) 회원가입 API에 `thirdPartyConsent` 저장 로직 추가, 미들웨어에 `/admin` 경로 추가~~ **완료 (2026-07-20)**
2. 인증 3종 세트: 랜딩 → 로그인 → 회원가입 (화면-API 연동, NavBar 포함)
3. 골프장 공공데이터 연동 재작성(스크립트 신규 작성) → 골프장 목록/상세
4. 기상청 날씨 API 연동
5. 스코어 등록 Step1/Step2 → 스코어 조회 → 라운드 상세
6. 마이페이지
7. 관리자 화면 4개
8. 테스트 → 배포
