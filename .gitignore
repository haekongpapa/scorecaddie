# app/
app/node_modules/
app/.next/
app/.env
app/.env*.local
!app/.env.example

# db (postgres data volume is docker-managed, not a local folder, so nothing here needed)

# 계정/시크릿 (2026-07-21: to-do.txt에 있던 Google OAuth secret이 GitHub push
# protection에 걸려 여기로 분리 — 실제 계정정보는 credentials.local.txt에만 보관)
credentials.local.txt

# OS
.DS_Store
Thumbs.db

# logs
*.log
